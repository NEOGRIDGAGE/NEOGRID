# Extension Guide

## New Consensus Modules
Add them as extension modules without changing the locked core.

## New Execution Modules
Add deterministic execution stages that preserve the state model.

## New Validation Layers
Add external validation and conformance checks without altering core logic.
