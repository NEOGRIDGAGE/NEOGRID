# NDAP Reference Implementation

This is the canonical implementation used for behavioral comparison only.
It is not authoritative for protocol definition.

The protocol definition lives in `../spec/NDAP_YELLOW_PAPER.md`.

Link to the existing NDAP core engine in the repository source tree.
