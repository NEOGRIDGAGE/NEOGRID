# NDAP Rust Client

Skeleton structure for a deterministic NDAP client implementation.

## Layout
- Cargo project scaffold
- client interface definition
- deterministic state machine mapping

This is a structure only, not a full protocol rewrite.
