# Rust Client Structure

A compliant Rust client should expose:
- state machine module
- networking module
- consensus module
- trace replay module

It must match NDAP client behavior under the canonical spec.
