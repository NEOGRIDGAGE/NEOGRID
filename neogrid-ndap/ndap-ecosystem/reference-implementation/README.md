# Reference Implementation

This directory contains the NDAP gold standard implementation used for validation only.

It is not the canonical source of truth. The canonical source of truth is the NDAP Yellow Paper in `../spec/NDAP_YELLOW_PAPER.md`.

Other clients MUST match the behavior of the reference implementation for conformance.

Core engine link:
- NDAP core engine in the repository source tree
