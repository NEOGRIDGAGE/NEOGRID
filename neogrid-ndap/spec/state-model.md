# State Model

NDAP state is defined over SMT/MMR roots and consensus metadata.

## Invariants
- finalized state is immutable
- no conflicting finalized roots at the same height
- state transitions preserve determinism
