# NDAP Ecosystem Overview

NDAP is a protocol, not a single codebase.

- The reference implementation defines canonical behavior.
- Clients are interchangeable if they satisfy the client specification.
- Conformance tests define correctness across implementations.
- The ecosystem is designed for extensibility across languages and runtimes.

This structure separates specification, implementation, clients, and validation into a public protocol ecosystem.