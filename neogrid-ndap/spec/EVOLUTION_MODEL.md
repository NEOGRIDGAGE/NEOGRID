# Evolution Model

The system evolves only through:
1. Versioned upgrades (v1 -> v2 -> v3)
2. Backward-compatible extension layers
3. Optional modules that do not touch the versioned deterministic consensus core

CORE = versioned deterministic consensus core
EXTENSIONS = mutable
